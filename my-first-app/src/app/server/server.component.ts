import { Component } from '@angular/core' //the core package gives us access to the core functionalities

@Component({
  selector: 'app-server', //the html tag that we will use the component later in other component templated
  templateUrl: './server.component.html'
}) // add a decorator to enhance your classes, know to TS now because we imported it
export class ServerComponent{ //not just a normal TS class, but a component

}
